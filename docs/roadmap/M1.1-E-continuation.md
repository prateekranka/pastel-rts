Continue M1.1-E in /home/bobbyranka/Projects/pastel-rts-m11-runtime from existing valid edits. Do not restart or discard anything. Same explicit gpt-5.6-luna/openai-codex/max fast OFF routing and original role/scope. Coordinator feedback on current scripts/qa-bundle.ts: default scenario.reproduction path filters/slices but does not call sanitizeSteps, so local paths/secrets/oversized strings can escape via the default branch even though explicit --steps is protected. Fix and exercise observable refusal. Identity revision is String(unvalidated input) and nested pack/scenario/map titles could carry arbitrary local paths; ensure bounded artifact generation excludes private local configuration and never reads/emits credentials. qa-scenario assetHashes=[] must be clearly labelled JSON-only where no asset manifest is supplied; never present that identity as a byte-complete published revision. Check actual map/scenario content matches the selected pack record rather than just ID presence. Finish focused validation and commit the coherent runtime checkpoint with a precise handoff; do not hide remaining gaps. All original acceptance requirements remain. Do not add security-disabling browser flags or bypass any tool denial. Backend A2 and Foundry D are now integrated in coordinator branch but do NOT merge them or edit their files in your exclusive worktree. Your APIs still follow the wave2 contract.