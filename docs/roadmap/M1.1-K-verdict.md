# M1.1-K independent final verdict

Decision

CONDITIONAL LOCAL ENGINEERING PASS.

The K correction is supported. The earlier claimed solid-magenta render defect was a false finding from a masked Playwright screenshot. The unmasked studio captures show the teal Diagnostics panel and normal game content. No confirmed M1.1 product engineering blocker remains in the reviewed evidence.

This is not an unconditional milestone sign-off. One native-pause timing verification remains pending after an intermittent failure under concurrent browser load. Human design acceptance, physical-device validation, and current external CI remain separate gates.

Review boundary

- M1.1 only.
- Product source reviewed at d23642d841c00b937af28e997c92523eea2eec0d.
- The later L change is the approved framing snapshot reconciliation at 9a17c16.
- No broad test rerun was performed for this correction.
- K added only this verdict and docs/roadmap/M1.1-K-artifacts/**.
- No product source, test source, configuration, simulation, navigation, package, security, or profile files were changed by K.

Artifact provenance correction

The framing assertion uses this intentional mask:

- File: apps/game-web/e2e/interaction-lab.spec.ts
- Lines: 278-282
- Mask: page.locator('.pastel-hud')
- Threshold: maxDiffPixelRatio 0.02

Playwright's default mask color is magenta. The magenta region in the historical masked framing image is not game output.

I inspected the unmasked captures at both required studio viewports:

- docs/roadmap/M1.1-K-artifacts/unmasked-studio-1280x800.png
- docs/roadmap/M1.1-K-artifacts/unmasked-studio-1194x834.png

Both captures show the teal Diagnostics panel. Both show the game scene, minimap, Army Rail, and player HUD. Neither contains a solid magenta rectangle or render block. The 1194x834 capture shows the corrected scrollable Diagnostics panel above the Army Rail. The panel occupies substantial world space. That is a human design concern, not a confirmed rendering failure.

The exact source sidecars and SHA-256 values are recorded in:

docs/roadmap/M1.1-K-artifacts/PROVENANCE.md

Snapshot disposition

The original framing failure was real as a test result but not a product regression:

- 31,101 pixels differed.
- Reported ratio: 0.04.
- Unchanged test tolerance: 0.02.
- L localized the complete diff to x=12..531 and y=279..439.
- The changed area contains the G bug-bundle row, its help text, and the shifted revision controls.
- Terrain, camera, entities, buildings, minimap, and gameplay geometry were not in the diff.

L reconciled the approved PNG at the unchanged 2% threshold. The L handoff records a passing framing assertion. The test mask and threshold were not relaxed. No K action changed the snapshot.

Functional evidence

The prior independent execution and preserved coordinator evidence establish:

- Full typecheck passed.
- Configured game-web lint passed.
- Full unit tests passed: schema 40, simulation 19, navigation 21, content server 18, Foundry 9, game-web 91.
- Game and Foundry production builds passed.
- Content validation passed with decoded PNGs, valid frame references, and resolved scenario dependencies.
- Fresh G/H browser run passed all 6 tests:
  - real pointer move bug-bundle export;
  - fresh-context exact checksum replay;
  - historical revision rejection and active-state preservation;
  - published studio load and exact asset identity;
  - draft isolation, stale publish rejection, and cosmetic replacement;
  - rules restart acknowledgement, reconnect, historical pin, and launcher values.
- Fresh Foundry M1.1 API and UI workflow passed both tests.
- Production offline preview passed with the content server stopped.
- Preserved interaction-lab behavior passed all behavioral tests. The only earlier failure was the framing snapshot described above; L reconciled that baseline separately.

The runtime evidence includes exact revision, pack, content, visual, rules, scenario, seed, and asset-prefix identity. Runtime errors and missing asset counts were clear in the captured sidecars. The bug bundle contains a real accepted move, a non-empty command stream, nontrivial checksums, fresh-context exact replay, and historical revision rejection.

Remaining gates and limits

1. Native pause remains pending.

   The correction request records one unchanged native-pause timing failure under concurrent browser load. A clean rerun without concurrent browser load is required. If the assertion fails again, it is a M1.1 engineering defect. K does not hide or waive this gate.

2. Human design acceptance remains pending.

   The Diagnostics panel is readable and scrollable at the reviewed viewports, and the prior overlap is fixed. The panel still uses substantial world space. Final hierarchy, density, and visual taste require the designated human review.

3. Physical-device validation remains pending.

   The captures use Linux Chromium SwiftShader. Their low desktop software-renderer timing does not prove or disprove the iPad target. K makes no 60 FPS or p99 device claim.

4. External CI remains pending.

   This local review does not certify current remote CI or merge state.

Source preservation

The committed history from d23642d to the current L checkpoint contains the approved framing PNG and L evidence only. No M1.1 runtime, simulation, navigation, or Foundry product source changed in that range. Existing unrelated worktree changes were left untouched.

Final recommendation

Keep M1.1 at CONDITIONAL LOCAL ENGINEERING PASS.

Do not make product edits for the withdrawn magenta finding. Do not lower the screenshot threshold. Complete the isolated native-pause rerun and the separate human/device/CI gates before declaring unconditional M1.1 completion.
