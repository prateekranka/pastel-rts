# M1.1-B: original animated fixture and packaged offline foundation

Repository and sole write root: /home/bobbyranka/Projects/pastel-rts-m11-fixtures. This is pastel-rts, NOT spacepixelrts. Use absolute paths and explicit terminal workdir. Do not search or change any other game repository. Continue existing valid work in this worktree. The owner profile's default game project is unrelated to this assignment.

Implementation routing: gpt-5.6-luna/openai-codex, requested max, fast OFF, latest explicit user override. Do not delegate or launch models. No profile/config/security/skills/memory/cron changes. M1.1 only. Read preserved docs/roadmap/00-astra-luna-contract.md and 01-milestone-1-1-designer-workbench.md, README, docs/architecture.md, existing schema and runtime content loaders before changes.

Prerequisite 6be785fe33429cae42f6d4dabd15660a91309827. Exclusive allowed writes: content/dev-pack-v2/**, scripts/**, root package.json/package-lock.json, apps/game-web/vite.config.ts, and docs/roadmap/M1.1-B-handoff.md. Do not touch app source, content-schema, server, simulation/navigation, Foundry, CI, numbered uploads or user's dev pack. Schema/server owned by concurrent lane A. No new dependency unless necessary; preserve npm workspace structure.

Goal: deliver tiny original fixture pack with two genuinely animated units, large walker explicitly marked proxy, two buildings; relative-path production web and iOS packaging; root dev:studio and content:validate commands. Implement only this bounded foundation, not later scenario/bundle commands or editor.

Use existing content v2 schema unchanged. Retain stable existing IDs and scenarios when adding content. All original generated art must be reproducible by an included generator and contain actual distinct motion frames, not repeated stills or fabricated facing. Label any proxy clearly. No downloads or borrowed copyrighted assets. Preserve original sources. Supply an explicit deterministic fixture scenario using existing schema; runtime integration lane will wire UI later if needed. Produce at least two animated units with working schema-valid clips, large walker proxy and two buildings, with visible missing-frame failures.

Packaging: inspect Vite source-copy preview behavior; production verification must read packaged dist, never a repo-source fallback masking missing assets. Include pack/assets and worker files with relative URLs. ios:sync-web keeps all required assets. No /dev-content or SSE dependency in packaged fixtures. No native code changes.

dev:studio: one root command starts game, Foundry and local content server; strict ports, readiness/errors, signal and child exit handling; clean shutdown of owned children only. Configurable ports and temp content directory for isolated verification. No external bind widening. Document actual URLs and env/CLI options in handoff. Content validate command accepts explicit pack directory and returns nonzero for invalid references, clips/assets, corrupt PNG, path traversal and limits using existing schema where possible; no private file reads outside selected pack. Test commands against isolated fixture copies. Scope allows new tests for these milestone requirements under scripts; root verification must execute them if added. Root lockfile exclusively yours. No QA scenario/bundle implementation in this lane.

Run relevant typechecks/tests/build, execute generator and validator, inspect packaged output, exercise studio startup/shutdown and port collision. Commit passing coherent checkpoint prefixed M1.1-B. Handoff includes commit, changed paths, reproducible commands/results, source attribution (original), exact fixture URLs, compatibility notes, outstanding browser/device verification. Do not claim physical iOS tests on Linux. Stop after this task.
