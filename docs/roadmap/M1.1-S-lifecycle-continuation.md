Continue existing S state without restarting source work. Same sole worktree /home/bobbyranka/Projects/pastel-rts-m11-texture-lifetime, explicit workdir every terminal call, Luna/max/fastOFF, no other models/delegation/security edits.

Coordinator source hypothesis to test, not a claimed runtime fact: SpriteAtlasCache.loadPack clears/disposes cache entries before UnitRenderSystem clears batches at async load completion. ensureLoaded also disposes a placeholder per asset while a batch can still reference it until all loads complete. Three can re-upload a disposed Texture if a live material still references it on a later frame, leaving an allocation that no subsequent owner disposes. This could explain variable+5 textures with stable geometry. Inspect BuildingRenderSystem for the same binding/disposal order.

Design invariant: no material drawn by the scene may retain a texture that its cache has disposed. Retire/rebind render batches before retiring referenced textures. Prefer an atomic resource handoff that keeps old art valid while replacements load; do not merely dispose everything repeatedly, hide renderer counts, or force GC. Verify hypothesis with actual identities/lifetimes and a short replacement run. Preserve needed generation cancellation guards and sim continuity. Smallest directly proven fix, existing focused tests, real resource-count return, own scoped commit/handoff. If the evidence disproves this hypothesis, follow the evidence and report it. Original S brief and gates remain binding.
