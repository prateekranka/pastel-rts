# M1.1-Q required replacement/leak soak

Read-only product scope in /home/bobbyranka/Projects/pastel-rts-m11-soak. gpt-5.6-luna/openai-codex/requested max fast OFF. No delegation/other models/profile/skill/memory/cron/security changes. No product/test/source/config edits and no committed new test/helper files. Temporary direct runtime probe is permitted only to execute this contract-required verification. Commit Q-prefixed evidence/docs only. Ports30373/30374/30387; unique disposable copy of the original content pack. Explicit absolute paths and terminal workdir. Stop owned services on success/failure; preserve all evidence.

Read numbered contract lines64-66: repeated replacements AND a leak soak are required. Existing H covers only two cosmetic replacements; short baseline soak is not a20-minute replacement leak observation. Exact final product source8a84af0 now passes both remote CI runs33978204772/33978202051, including32developer tests, separateofflinepreview and nativecompile. Do not redo those passing gates.

Perform one actual20-minute fixed-seed interaction-lab studio soak at1280x800, WebGL, DPR1, same original scenario/entity count. Drive the real runtime and content API, not mocked counters. After baseline warmup, alternate the scout's original PNG and the compatible gravemark-stalker PNG through real draft/publish at least10times over the soak; cosmetic visual identity must change, simulation rules must remain equal, and runtime must adopt/acknowledge each exact revision. Keep entity count, seed, camera/quality controls fixed for comparable measurements. Capture baseline, replacement checkpoints and final screenshot/sidecars.

Sample frame distribution/FPS, simulation and navigation timing, draw calls, live renderer texture/geometry counts, active revision, missing assets/errors, and JS heap when available. Compare warmup baseline and post-replacement settled counts. Use existing diagnostics/report APIs where possible. Do not call CPU submission time GPU time. Do not force a passing heap result; report resource growth or missing metrics honestly. A steady resource count is not a proof against every leak. LinuxSwiftShader numbers cannot certify iPad60FPS/p99.

Long execution must be a bounded owned background process with actual completion status, not CLI self-report. Finish only after20minutes elapsed and capture final data. The coordinator will independently read your artifacts and verify duration/resource evidence. If any clear leak, repeated error, revision mismatch or retention failure is found, report it as a blocker without unowned fixes. No model/profile changes or false device claim. Write docs/roadmap/M1.1-Q-handoff.md with exact commands, duration, fixture/ports, model/session IDs where available, resource table and limits. Commit evidence with M1.1-Q prefix. M1.1 only; no optional experiment or later milestone.