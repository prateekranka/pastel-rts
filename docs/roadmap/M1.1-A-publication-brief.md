# M1.1-A: safe content publication foundation

Role: implementation only. Latest explicit user routing overrides the numbered uploads: gpt-5.6-luna/openai-codex, requested max reasoning, fast OFF. Do not delegate, launch other models, modify profiles/skills/memory/cron, or disable security. Preserve configured Git identity. Only M1.1 is active.

Read docs/roadmap/00-astra-luna-contract.md and 01-milestone-1-1-designer-workbench.md (preserve verbatim), README.md, docs/architecture.md, docs/milestone-1-contracts.md, packages/content-schema/src, tools/content-server/src, apps/foundry/src/api/contentApi.ts (read-only). Base 6be785fe33429cae42f6d4dabd15660a91309827.

Goal: implement safe versioned content publication, immutable source preservation, conflict detection, revisions and rollback on the existing local server. This is an independently reviewable first foundation, not the whole milestone.

Allowed writes: packages/content-schema/** and tools/content-server/** only, plus docs/roadmap/M1.1-A-handoff.md. Do not edit root lockfiles/package scripts, apps, runtime, simulation, navigation, fixtures outside temp dirs, or numbered uploads. No public services or deployment. Existing server bind remains local.

Architecture contract: retain existing v2 unit/building API compatibility. Authoring changes are drafts; runtime publication is explicit. Add GET /v2/revisions (immutable revision metadata), POST /v2/publish (expected revision precondition), POST /v2/revert (target revision plus expected current revision). GET /pack must serve the current published revision after this workflow is established, with a compatible initial migration for existing content. Include revision, visual content hash, simulation-rules hash and an explicit restart requirement. Same stable ID replacements preserve original images in immutable source storage. Revision metadata must reference immutable manifests/assets; never delete referenced sources. Rollback creates a new publication pointing to retained content. Draft APIs may retain existing paths. Failed validation/publication must leave previous publication fully usable. Reject stale mutations with HTTP 409 and actionable current revision metadata. Add reference attachment metadata separate from runtime assets, duplicate, enable/disable and dependency-aware removal primitives only as needed by the existing editor; document exact endpoints and payloads in handoff for the later UI lane. Shared schema ownership is exclusive to this task.

Invariants: validated versioned content, stable IDs, no executable imports, decoded PNG validation (not header-only), byte/pixel limits, traversal rejection including encoded paths, missing animation-frame rejection, deterministic canonical hashes, atomic publication, last good revision remains readable across failures. Rules/footprints never silently mutate an authoritative running scenario. References never become runtime art. Do not fabricate animation. Preserve M0/M1 tests and migration behavior.

Tests authorized within allowed packages for these observable requirements: corrupt/invalid PNG, oversize images, traversal, stale save/publication, invalid frames, dependencies, publication failure, immutable source replacement and revert, restart/read-back persistence. Tests use isolated temporary packs. Run relevant existing and new tests and typechecks. Do not weaken tests. Commit a passing coherent checkpoint with M1.1-A prefix. Report commit, changed files, commands and actual outputs, API payloads and known limitations in handoff. Stop after this bounded task; do not start UI or roadmap work.
