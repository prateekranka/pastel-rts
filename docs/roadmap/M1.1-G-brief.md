# M1.1-G actual non-coder bug reproduction path

Sole worktree /home/bobbyranka/Projects/pastel-rts-m11-bundle. gpt-5.6-luna/openai-codex/max fast OFF. Use explicit workdir/absolute paths. No delegation, other models, profile/skill/memory/cron edits, security disabling flags or bypass of tool denials. Read the two numbered M1.1 contracts, wave2 contract, M1.1-E-handoff.md and M1.1-runtime-export-review.md. Preserve source and sim/navigation/native code. Do not alter numbered uploads or PROGRESS.md. Commit own work M1.1-G prefix; no push. No later milestone.

Implement only the missing bounded runtime bug export/reproduce path. Own apps/game-web/src/qa/bugBundle.ts, new qa/bugBundle.test.ts, apps/game-web/src/sandbox/createInteractionLab.ts for UI wiring, apps/game-web/e2e/m11g-bundle.spec.ts for observable user path, and package.json only to add qa:scenario/qa:bundle scripts invoking E's real TS scripts with tsx. Do not edit E's CLI scripts, PublishedContentClient, GameApp, HUD, schemas, backend, Foundry, other e2e files, lock or core simulation/navigation. Read those as needed. H independently owns runtime acceptance and content-client integration. If you require a non-owned file, report exact dependency rather than edit it.

Design: existing ScenarioController revision-aware save/replay is the source of truth. Add accessible Export bug bundle and Reproduce bug bundle file controls to existing lab, plus short human-readable reproduction steps. A bounded versioned bundle must contain a validated scene/map/scenario/pack definition snapshot sufficient to replay, seed, exact ordered commands/checksums/tick range, revision and hashes, allowlisted bounded diagnostics, and bounded steps. Never read arbitrary paths from bundle data or include private local paths/credentials. Refuse excess limits rather than silently truncate reproducibility-critical commands. Clearly state JSON-only coverage unless asset bytes are actually included and hashed; don't invent full asset identity. Verify active/historical content against the bundle and refuse unavailable/tampered revisions visibly while preserving active scene. Reproduce in a fresh browser context with same validated scenario/commands/seed and compare actual checksum sequence, not true/false shape. Use current exact content version for normal path; refusal for unknown/mismatched content must be explicit. No combat, new sim systems or universal inspector.

Root scripts qa:scenario and qa:bundle need documented exact CLI input examples; reuse scripts already present. Unit tests must cover bounds, identity tampering, unsafe/private diagnostics and steps including default reproduction, and preservation on failed import. One real UI export/download -> fresh-page import -> matched replay acceptance; bad bundle leaves active state intact. Run direct normal Playwright with no explicit --no-sandbox or --enable-unsafe-* flags; reuse repository launch configuration and only documented ignoreDefaultArgs adjustment if needed. Isolate ports 21373/21374/21387 and temp pack. Preserve screenshot/bundle/command output under docs/roadmap/M1.1-G-artifacts. No fabricated results. Keep task focused; report other deficiencies without broadening ownership.

Hand off exact commits, commands/results, captured artifact paths, guarantees and limitations. Human/device gates remain pending. Coordinator will independently rerun after integration.
