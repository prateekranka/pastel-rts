# M1.1 coordinator runtime export review

Source reviewed in current runtime worktree before its final handoff. This is not an executed exploit claim.

Existing apps/game-web/src/qa/bugBundle.ts still defines schemaVersion 1 with packId/packHash and commands/checksums, but no embedded scene/map/scenario, publication revision, or reproduction steps. Arrays are unbounded. Diagnostics allowlisted string values are not length/private-path bounded. The runtime UI export must meet the milestone, not only the separate CLI exporter.

Before acceptance, a worker must implement or demonstrate a bounded runtime export/import/reproduction path that preserves scene identity and deterministic commands/checksums. Exercise it from the actual running UI and reproduce in a fresh page/process. Reject tampering, missing content, and sensitive diagnostics. A CLI artifact alone does not prove the non-coder browser export path.

Other outstanding integrated checks: draft initial load versus published initial load; live art replacement and resource disposal; simulation-rule restart acknowledgement; revision A/B comparison with the same commands/seed; baseline tests with current expected art reviewed before snapshot updates.
