# M1.1-R handoff

Status: ENGINEERING PASS for the missing favicon resource.

## Change

- Added `apps/game-web/public/favicon.svg`.
- Added an explicit relative `image/svg+xml` link in `apps/game-web/index.html`.
- The icon is a small original static isometric diamond. It uses the existing dark-teal, cream, and coral palette. It has no script and no external reference.
- No game, UI, simulation, test, snapshot, security, configuration, or `PROGRESS.md` file changed.

## Evidence

Full focused evidence is in `docs/roadmap/M1.1-R-artifacts/verification.json`.

- Production preview on `31375` loaded the game page.
- The browser requested `/favicon.svg`; the request returned HTTP `200`, `image/svg+xml`, 313 bytes.
- The browser made no `/favicon.ico` request.
- Production preview console messages: `0`. Uncaught JavaScript errors: `0`. Page errors observed: `0`.
- The existing dev check on `31373` also requested `/favicon.svg`, made no `/favicon.ico` request, and reported zero console/page errors. That check was not rerun.
- The passed build and iOS sync were not rerun. The built and copied icon were verified at 313 bytes with the same SHA-256:
  `9a694950d033c06cb73be0b97ba7cfa16521484f92b065a17935e6f0464b49c7`.
- Both generated `dist/index.html` and the iOS web copy contain the relative `./favicon.svg` link.
- Owned ports `31373` and `31375` were stopped and read back free.

## Handoff boundary

- Do not rerun the completed 20-minute replacement soak.
- Do not claim physical-device validation.
- The generated `apps/ios-shell/PastelRTS/WebGame/.gitkeep` change remains uncommitted and unstaged by request. It was preserved.
- The final source and R documentation are in the `M1.1-R` commit containing this handoff.
