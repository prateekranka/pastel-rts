# M1.1-E runtime checkpoint handoff

Status: bounded M1.1-E implementation checkpoint. This is not full M1.1 acceptance.

Repository: /home/bobbyranka/Projects/pastel-rts-m11-runtime
Branch: m1.1/runtime
Parent before this checkpoint: 5c51e35bca16a56f5e71d544e352048f0801c5a9
Checkpoint commit: the M1.1-E commit that contains this file. Verify with `git rev-parse HEAD`.

Scope completed

- Added the worker-owned runtime connection boundary in `PublishedContentClient`.
- Kept the bundled relative fixture as the default runtime source.
- Added explicit `?content=studio` selection for the published-content path.
- Added revision, pack, metadata, content-hash, visual-hash, and simulation-rules-hash validation.
- Added initial load, bounded refresh, EventSource publication handling, reconnect state, acknowledgement calls, disposal, visible failure state, and last-good-content preservation.
- Added explicit pending-revision handling. A simulation-rules change requires explicit restart.
- Added generation-aware runtime content replacement and renderer/asset reconciliation.
- Added revision-aware named scenarios, seed controls, save/load/import/export/reset state, replay identity, command results, and bounded A/B revision comparison.
- Added worker navigation timing and runtime diagnostics. Added renderer resource counts and HUD visibility for revision, hashes, scenario, seed, content phase, errors, and performance fields.
- Updated pack-driven unit, building, palette, placement, atlas, and entity-registry paths.
- Added deterministic `scripts/qa-scenario.ts` and `scripts/qa-bundle.ts` checks for bounded JSON, safe revisions, exact pack references, content hashes, replay bounds, path/private-data rejection, and JSON-only asset coverage.
- Added `docs/roadmap/M1.1-E-runtime-spec.md` as the implementation contract.

Preserved and excluded scope

- No changes to `packages/simulation` or `packages/navigation`.
- No changes to the content schema, content server, Foundry, A2, D, editor projection, native bridge, root package files, or lockfiles.
- No changes to core camera math, combat, economy, fusion, folding, or later milestone work.
- No A2 or Foundry D files were merged or edited in this worktree.
- No production deployment or physical-device claim is made.

Passing validation

1. `npm run typecheck`
   Result: exit 0. Content schema, simulation, navigation, content server, game web, and Foundry typechecks passed.

2. `npm run build`
   Result: exit 0. Game web and Foundry production builds passed.

3. `npm test --workspace @pastel-rts/game-web -- --run`
   Result: exit 0. 25 test files passed. 76 tests passed.

4. `npm run lint --workspace @pastel-rts/game-web`
   Result: exit 0.

5. `npx eslint --config apps/game-web/eslint.config.js --no-ignore scripts/qa-scenario.ts scripts/qa-bundle.ts`
   Result: exit 0.

6. Deterministic CLI check from the same checked-out inputs
   - Two `qa-scenario` runs passed with exit 0.
   - Scenario outputs were byte-identical.
   - Scenario output SHA-256: `5d9f59d44cb253b92f65a405a04c22769171c43473612bae6e9f5cc7463abd15`.
   - Two `qa-bundle` runs passed with exit 0.
   - Bundle outputs were byte-identical.
   - Bundle output SHA-256: `d5af76223a588ed6c03d2c736bc73219a2d839dc07c46f44772e3a386e2e1649`.
   - Scenario content and identity both declare `assetCoverage: "json-only"`.
   - Bundle content declares `assetCoverage: "json-only"` and `assetHashes: []`.
   - Valid scenario and bundle output contained no absolute path or URL markers.

7. Direct refusal checks
   - Default `scenario.reproduction` private-path input: exit 1, `qa:bundle failed: reproduction step 0 is unsafe`.
   - Explicit `--steps` private-path input: exit 1, `qa:bundle failed: reproduction step 0 is unsafe`.
   - Nested scenario display-name private path: exit 1, `qa:bundle failed: scenario definition.displayName contains unsafe text`.
   - Scenario path not matching the selected Pack v2 record: exit 1, `qa:scenario failed: Scenario or map file does not match the selected Pack v2 record`.
   - Requested content hash mismatch: exit 1, `qa:scenario failed: Requested content hash does not match the Pack v2 content hash`.
   - Traversal input path: exit 1, `qa:bundle failed: scenario path must be a local non-traversal path`.

Process and smoke handling

- Only the owned game-web and content-server processes were sent SIGTERM.
- Port 19373 had no listener after cleanup.
- Port 19374 had no owned listener.
- Port 19387 had no listener after cleanup.
- `/tmp/m11-e-runtime-smoke.mjs` was edited to remove `--no-sandbox` and `--enable-unsafe-swiftshader`. It was not run again.
- The previous smoke captures used the forbidden explicit browser override. They are not acceptance evidence and are excluded from this handoff.
- No further browser smoke was run against the old backend.

Remaining acceptance gaps

1. Coordinator-only backend integration is still pending. This worktree does not contain the integrated A2 or Foundry D changes. The following need central verification against the actual A2/D services: initial published load, metadata and pack retrieval, revision asset retrieval, acknowledgement read/write, SSE publication and hot reload, reconnect and resync, rules-change restart, cosmetic replacement, failure fallback, and disposal during an in-flight replacement.

2. No accepted browser evidence exists for this checkpoint. The old smoke captures are explicitly rejected because they used `--no-sandbox`. Linux/SwiftShader timing is not iPad or GPU evidence.

3. Focused runtime regression tests were not added. Replacement-after-dispose, repeated reload, reconnect, rules restart, same-seed replay, strict save/import rejection, row-generation reconciliation, and native pause timing still need direct observable test coverage.

4. Existing QA baseline defects remain unresolved in this lane: accepted blocker readback, replay mismatch after the blocker sequence, and the native pause delta. They are not hidden by this checkpoint.

5. `apps/game-web/src/qa/bugBundle.ts` remains unfinished. It still lacks scene, revision, and reproduction fields. It also lacks the required bounded-array and strict input validation surface. The coordinator must route this to a fresh owner or extend it centrally.

6. `qa-scenario` and `qa-bundle` use explicit `assetCoverage: "json-only"` with empty asset hashes. This is truthful JSON identity only. It is not byte-complete published asset identity. Actual revision asset-manifest verification remains a coordinator/backend acceptance task.

7. The earlier ad hoc standalone TypeScript command for both scripts exited 2 and is not a supported project check. Script ESLint and both CLIs execute successfully through the supported workspace environment. No standalone script `tsconfig` claim is made.

8. No device, iPad, GPU, production deployment, full milestone, or human design acceptance claim is made.

Coordinator next action

Use the checkpoint commit from this branch. Integrate only with the actual coordinator A2/D changes. Run the central API, SSE, asset, acknowledgement, replacement, and browser verification. Route `bugBundle.ts` and the listed runtime and baseline defects to fresh owners. Do not treat this checkpoint as full M1.1 completion.
