# M1.1-S unresolved texture growth

Sole worktree /home/bobbyranka/Projects/pastel-rts-m11-texture-lifetime. gpt-5.6-luna/openai-codex/requested max, fast OFF. No delegation/other models/profile/security/skill/memory/cron changes. Explicit workdir on EVERY terminal call. Preserve all existing artifacts and source work. R separately owns only index.html/favicon; do not touch those.

Coordinator read raw Q soak summary, not just CLI summary: /home/bobbyranka/Projects/pastel-rts-m11-soak/docs/roadmap/M1.1-Q-artifacts/summary.json lines2312–2373. After12 cosmetic publications over actual1,210,872ms, renderer textures rose7→12; geometries stayed17; entities25 and simulation-rules hash stayed fixed; V8 used heap fell. Do not claim no leak from these results. Diagnose whether the5 extra textures are leaked or a finite justified cache, and prove the result from actual live resources. Baseline has4unit atlas+3building textures; same final scene should not accumulate discarded atlas textures. Revisions4–15, alternate scout/stalker PNGs. Q raw artifacts contain every replacement receipt and unmasked screenshot. Existing helper /tmp/m11-q-replacement-leak-soak.mjs is a temporary probe; its art-ready helper has an object-vs-array shape bug, do not treat that as missing game art. Preserve it and failed attempts.

Read both numbered contracts, source paths and existing lifecycle tests. Find smallest root cause, preferably sprite/texture loader disposal or async late-load ownership. Allowed product scope only directly implicated texture lifecycle code in apps/game-web/src/sandbox/animation/spriteAtlas.ts, UnitRenderSystem.ts, BuildingRenderSystem.ts, createInteractionLab.ts, and content/PublishedContentClient.ts. If another file is truly needed, report precise ownership request. No sim/nav/engine design, schemas, content rules, camera/input/UI-layout changes. Existing directly related tests may be extended; no new test files. Do not change counters, hide resources, force GC to fabricate a pass, reduce fixture/entity counts, skip validation, or relax error/acceptance gates.

First reproduce with a focused shorter real replacement sequence using fresh disposable fixture and owned32373/32374/32387. Inspect resource lifecycle/identities and counts; distinguish warmup from continued per-cycle growth. Route fix through actual code and focused tests. A clear bounded cache explanation requires live evidence and justified ownership, not a guess. Save source/commands/measurements and a committed M1.1-S-handoff.md. If fixed, run a replacement series proving return to expected counts; coordinator will require an updated20minute soak for affected resource-lifetime cone, not redo unrelated gates. Keep raw evidence and stop/readback owned servers. Do not edit PROGRESS. Report real blockers; no iPad performance or overall engineering pass claim.
