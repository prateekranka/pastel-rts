# M1.1-R missing favicon resource

Sole worktree /home/bobbyranka/Projects/pastel-rts-m11-favicon. gpt-5.6-luna/openai-codex/requested max fastOFF. No delegation/other model/profile/security/skill/memory/cron edits. Explicit workdir on EVERY terminal call; CLI --in alone did not prevent a prior worker landing docs in the coordinator tree. Preserve all work.

The full 20-minute replacement soak completed with12 valid publications, stable entity/rules identity and zero missing game assets. Its one console error was the browser's GET /favicon.ico404, see coordinator docs and /home/bobbyranka/Projects/pastel-rts-m11-soak/docs/roadmap/M1.1-Q-artifacts/browser-events.json. Fix the missing resource, not the error collector. Do not rerun valid20minute soak.

Allowed product files: apps/game-web/index.html and one original tiny SVG favicon under apps/game-web/public/. Supply a real minimal isometric diamond icon in existing dark-teal/cream/coral palette; static SVG with no scripting or external references. Link it explicitly. No blank data URI, route suppression, error filters, test edits, other UI/game/sim changes, or snapshot changes. This is a missing-resource repair, not a branding project.

Verify actual browser auto-request serves icon200, no favicon.ico404, console/page error arrays empty in dev and production preview, and icon included by build and iOS web copy. Use31373/31374/31387 if full studio needed, otherwise game31373 only; production can use31375. Keep owned servers isolated and stop/readback ports. Existing direct browser checks only, no committed test files. Do not claim physical validation. Save evidence and concise M1.1-R-handoff.md; commit own narrow paths with M1.1-R prefix. Do not edit PROGRESS. Keep within35 tool turns; prioritize actual focused checks and commit, no broad gate reruns.
