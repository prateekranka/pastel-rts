# M1.1-M CI mode correction

Sole worktree /home/bobbyranka/Projects/pastel-rts-m11-ci-mode. gpt-5.6-luna/openai-codex/requested max fast OFF. No delegation/other models/profile/skill/memory/cron/security changes. Allowed edits: .github/workflows/ci.yml and docs/roadmap/M1.1-M-* only. No product, snapshot, threshold, test-source or Playwright-config edits. Existing CI runner only; no new infrastructure, billing, credentials or permissions. Do not push; commit your own work. Use explicit workdir/absolute paths and free ports28373/28374/28387 for local probes. Preserve artifacts.

Root9a17c16 full developer-mode suite passed32tests, with1preview-onlyskip. The offlineproduction test passed separately with contentserver omitted. Remote CI run33975185566 at same9a17c16 failed because current CI implicitly selects preview mode for the ENTIRE authoring suite (playwright config defaults CI=true to preview). Failures: compiled Foundry links to baked5173 but harness expects4173 (3related failures); production-specific authoring controls differ from the dev framing snapshot (62,210pixel/7% diff); studio lifecycle tests are skipped in preview. This is not solved by loosening assertions or regenerating snapshots. Read actual source config, workflow, Foundry Vite origin handling, mode guards in tests. Logs available main repo docs/roadmap/M1.1-ci-mode-failure.log once copied. Root source contracts define developer studio versus standalone offline build; execute both modes correctly.

Make the existing full Visual and interaction tests step explicitly run PLAYWRIGHT_SERVER_MODE=dev, exercising actual studio/runtime/publication/Foundry tests with isolatedJfixture. Add a focused production-preview offline verification step using the existing m11c-baseline test, PLAYWRIGHT_SERVER_MODE=preview and PLAYWRIGHT_SKIP_CONTENT_SERVER=1. Preserve current typecheck/tests/build/nativecompile/securitypermissions/artifact upload gates. Do not hide failures, skip functional tests, lower thresholds or use update-snapshots. No blanket CI bypass. Build already occurs before browser steps; use actual built output for offlinecheck.

Validate YAML structure and run the two exact mode-correct commands locally with CI=true, isolatedports, default fixture, supported systemChromium and ignoreDefaultArgs workaround. Developer suite should32pass/1previewskip; separatepreview should1pass. If other failures arise, report them, no unowned edits. Handoff records mode-rootcause, commands/results and remaining external CI/device/human gates. Native/physical behavior is not certified by localLinux. Commit M1.1-M prefix. Stop at M1.1.