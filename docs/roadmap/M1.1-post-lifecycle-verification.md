# M1.1 post-lifecycle coordinator checks

Product commit: 6e4d580, including the S texture handoff and R favicon. These are coordinator-executed checks, not only lane reports.

- Root typecheck, lint, tests, build, iOS web sync and copied-pack validation passed. Game-web has92 unit tests; schema40, simulation19, navigation21, content-server18 and Foundry9 passed.
- Full developer Playwright suite passed32 tests with no retries; its one production-only check is intentionally separate.
- Separate production-preview/offline check passed1 test with content and Foundry servers disabled. The first attempt used a stale grep title and selected no tests; the corrected exact title is `production preview boots with content server stopped`.
- Root dev browser automatically requested favicon.svg, HTTP200 image/svg+xml,313bytes; no favicon.ico request and no console/page errors. The owned Vite child was terminated and port14373 read back free after the process wrapper alone did not reap it.
- Actual artifacts: M1.1-post-lifecycle-artifacts/ and M1.1-post-lifecycle-offline-artifacts/ in this directory. The offline sidecar retains its original /tmp screenshot path and capturedAt2026-09-05T18:24:30.522Z; PNG and sidecar were copied intact from that actual run.

Artifact routing finding: m11f-visual.spec.ts assigns process.env.M11C_ARTIFACT_DIR from M11F_ARTIFACT_DIR during test discovery. Set BOTH values to the desired destination, even when grepping only a C test. Setting only M11C can route its capture to /tmp/pastel-m11f-artifacts. This did not change the executed test result. Future captures use matching variables; old raw files are not rewritten to disguise the path.

Q2 long resource-lifetime retest and independent V review remain pending. No overall engineering, human-design or physical-device pass is claimed here. Prior Q/S raw blocked summaries and their probe defects remain preserved.
