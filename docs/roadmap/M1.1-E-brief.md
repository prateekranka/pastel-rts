# M1.1-E runtime, replay and reproducible QA

Sole repository /home/bobbyranka/Projects/pastel-rts-m11-runtime. NOT spacepixelrts. Explicit workdir/absolute paths. gpt-5.6-luna/openai-codex/max, fast OFF. No delegation/other model launch/profile/security/skills/memory/cron changes. Base aab0981. Read numbered contracts, M1.1-wave2-contract.md, A/B handoffs, M1.1-source-review.md, actual GameApp/content/ScenarioController/replay/worker source. Preserve Git identity.

Own apps/game-web/src/** EXCEPT camera/previewProjection.ts and existing core camera math; own scripts/qa-scenario.ts and scripts/qa-bundle.ts only. No root package/lockfile edits (A2 wires script names). No packages/simulation or navigation changes, server/schema, editor, e2e, native, fixture, or other scripts. Tests for milestone runtime behavior allowed in app. Fix root causes in allowed scope, report if preserved sim needs separate scoped decision.

Deliver runtime connection to actual published Foundry content, initial and hot reload, revision acknowledgements, reconnect, failure visibility and disposal. Wave2 API contract is exact; implement client against it while A2 backend runs independently. ?content=studio is explicit development path; default packaged build must load relative original fixture without SSE or /dev-content. A selected revision pins metadata/pack/assets. Rules changes mark restart required with explicit action, never silently mutate an authoritative match. Pure visual changes may apply cosmetically with generations guarding async replacement. Reload must refresh actual runtime archetype/render state rather than only bundled pack. Content load failure must remain visible; do not silently label rectangles as final art. Last good revision remains usable.

Named scenario presets, seed and revision identity controls, save/load/replay/reset in actual runtime UI. Bounded A/B comparison of two immutable revisions with same scene/map/seed/command log using existing deterministic simulation/replay; record both content/rules hashes and checksums. Rules differing can yield different results, not a false equality assertion. Save document must contain/check exact content revision/hash, map and future-affecting replay inputs. Reject unknown/mismatched imports rather than use current unrelated map/pack. No arbitrary code evaluation. qa:scenario and qa:bundle must be actual CLI implementations accepting documented explicit inputs, nonzero on invalid/corrupt/traversal/hash mismatch, deterministic repeated output. Bundle allowlists bounded scene, commands, hashes, diagnostics and reproduction steps; exclude private local paths, credentials, unrelated files, remote fetches. UI export/import reproducibly reconstructs state from exported bundle or clearly explains missing immutable dependency; include safe exact selected content rather than arbitrary disk paths if self-contained artifact is needed.

Investigate QA baseline defects: accepted blocker not added to pickable readback, replay mismatch after blocker sequence, native pause delta. Follow IDs through worker transfer/registry and public API; do not weaken tests or alter sim rules. Runtime remains worker-owned deterministic ticks. Zoom does not change command availability. Reference images never become animation. Preserve WebGL baseline/WebGPU fallback and native lifecycle.

Expose bounded diagnostics needed for replacement soak: frame distribution, sim/nav timing, draw calls, texture/geometry counts and active revision, renderer context. Use existing diagnostics not duplicate universal editor. Tests for async replacement-after-dispose, repeated content reload, reconnect, revision restart, same-seed replay, import safety; preserve current tests. CPU submission is not GPU timing, Linux is not device proof.

Use isolated ports 19373/19374/19387 and temp pack. Backend APIs may await A2; no mock success claims. Run typecheck/lint/app tests and direct reproducible CLI checks. Commit bounded passing pieces M1.1-E prefix. Handoff docs/roadmap/M1.1-E-handoff.md includes exact APIs/inputs, commands/results, commits, known defects and non-coder steps. Only M1.1; no combat/fusion/folding/economy or roadmap advance. No full milestone or physical acceptance claim.
