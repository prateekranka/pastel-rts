# M1.1 provider recovery and resumed execution

Checkpoint: 853df6a on docs/roadmap-m1-1. No source restart or history rewrite.

Preflight found no prior Luna worker process or tmux session. G/H/I worktrees were clean at their existing checkpoint; their logs ended in HTTP 429 before implementation. Their assigned 213xx/223xx/233xx ports had no listeners. All existing uncommitted evidence and input files were preserved.

A real fresh request completed in Luna session 20260905_185350_ad6f16 and returned the requested smoke response. state.db records model gpt-5.6-luna, billing_provider openai-codex, requested effort max. No fast option or credential/profile reset. This demonstrates current request recovery, not unlimited future capacity or returned effort semantics.

Resumed original G/H/I sessions with explicit model/provider/max pins, no replacement context or duplicate writers:
- G: 20260905_184739_7b116b, bug bundle.
- H: 20260905_184739_a641e0, runtime publication gate.
- I: 20260905_184919_0e25b7, studio entry and walkthrough.
Recovery logs are M1.1-G-worker-recovery.log, M1.1-H-worker-recovery.log, M1.1-I-worker-recovery.log.

Important correction: the coordinator's previous isolated Foundry run used an empty temporary pack. Fixture-dependent failures from that invocation do not establish product defects. The fresh correct invocation copied content/dev-pack-v2 into the temporary pack first. Actual integrated Foundry run at 853df6a: all 9 tests passed in 40.4s, including stable-ID replacement, publication, preservation, reference and library workflows. No assertion or threshold changed.

Command:
PACK_DIR=$(mktemp -d /tmp/pastel-m11-foundry-recovery.XXXXXX)
cp -R content/dev-pack-v2/. "$PACK_DIR/"
PLAYWRIGHT_GAME_PORT=14373 PLAYWRIGHT_FOUNDRY_PORT=14374 PLAYWRIGHT_CONTENT_PORT=14387 PLAYWRIGHT_CONTENT_PACK_DIR="$PACK_DIR" PLAYWRIGHT_SERVER_MODE=dev PLAYWRIGHT_CHROMIUM_PATH=/usr/bin/chromium PLAYWRIGHT_IGNORE_DEFAULT_ARGS=--disable-dev-shm-usage npm exec playwright test -- --project=foundry --workers=1

Only M1.1 is active. Engineering, human design and physical-device gates remain separate and pending.
