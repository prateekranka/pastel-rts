# M1.1 remote native compilation evidence

GitHub Actions run 33965323173 at exact commit 6308242772c224b20e9a813dd2398c6fa6828b23:
https://github.com/prateekranka/pastel-rts/actions/runs/33965323173

Read back using authenticated gh run view (existing Git HTTPS credential, no new login/config or credential disclosure).

Job 101304310581, iOS simulator compile: success. Install, production game build, ios:sync-web, XcodeGen and xcodebuild all succeeded. Native job URL:
https://github.com/prateekranka/pastel-rts/actions/runs/33965323173/job/101304310581

This verifies native compilation at that checkpoint only. It does not verify simulator runtime launch, touch, physical iPad, or later commits.

The same run's web job 101304310697 failed at Visual and interaction tests. Typecheck, lint, unit tests, production build and browser installation succeeded. Overall CI is NOT PASS. Local integrated browser defects are recorded separately and routed to runtime/acceptance workers.

The local Linux host has no xcodebuild/xcrun/XcodeGen/Swift/sqim/asc executable from command lookup. Existing repository macOS CI supplied a real alternative for the native compile check; no new infrastructure or paid service was created.
