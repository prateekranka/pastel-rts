# M1.1-J reliable default browser fixture

Sole worktree /home/bobbyranka/Projects/pastel-rts-m11-ci-fixture. gpt-5.6-luna/openai-codex/max fast OFF. No delegation/model/profile/skill/memory/cron/security changes. Explicit workdir/absolute paths. Read numbered M1.1 contracts, current playwright.config.ts and F handoff. Own ONLY playwright.config.ts and docs/roadmap/M1.1-J-*; no product files, test suites, root package/lock or other docs. H owns runtime and e2e suite changes. Commit own M1.1-J work, no push.

Fix concrete default isolation gap: config uses fixed /tmp/pastel-foundry-e2e and mkdir without copying fixture. Existing CI default therefore lacks required checked-in scenarios/dependencies; repeated invocations also reuse mutated state. Manual properly seeded coordinator Foundry run passed all9; empty-pack invocation failed assumptions. Root-cause fix: each unconfigured run gets its own disposable copy of content/dev-pack-v2 before content server starts. Preserve explicit user-supplied pack directories and never silently overwrite or remove them. Parent and Playwright worker config evaluations must agree on same run directory and must not reseed a live mutable pack. Environment propagation must remain correct. Ensure default concurrent tests do not race shared mutable pack: serialize as needed or isolate per project; do not weaken assertions. Keep explicit offline skip-content-server path no content server or dependency.

Validate on this host with configured isolated ports 24373/24374/24387 and installed Chromium, documented ignoreDefaultArgs only, but no PLAYWRIGHT_CONTENT_PACK_DIR/CONTENT_PACK_DIR selection. Run existing Foundry suite twice as separate commands with fixture-generated defaults, proving fresh state. Existing current runtime popup may need H product fixes; report exact failure without editing H-owned files. Test explicit pack preservation with readback; do not create new test/helper files. No sandbox-disabling flags or bypass of denied commands. Hand off commands/results, directory behavior and outstanding integration gaps; no whole milestone pass. Finish this small root-cause piece and exit.
