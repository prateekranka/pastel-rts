# M1.1-F integrated Foundry and API acceptance handoff

Status: PASS for the integrated A2 content server and D Foundry workflow acceptance lane. The bounded test and configuration changes are implemented. No product source changed. Runtime E is not integrated or revision-acknowledged by this handoff.

## Scope and boundary

Repository: `/home/bobbyranka/Projects/pastel-rts-m11-acceptance`.

Base ancestry: `9e282fc` is an ancestor of the worktree. The pre-acceptance HEAD was `f3379e7` (`M1.1: scope fresh integrated editor acceptance`).

Allowed changes are limited to:

- root `package.json` Foundry test coverage;
- `playwright.config.ts` game-origin alignment;
- `apps/foundry/e2e/**` acceptance and legacy workflow coverage;
- `apps/game-web/e2e/**` replay assertion, visual capture, and approved snapshots;
- `docs/roadmap/M1.1-F-*` contract and handoff.

The test runs used a fresh temporary copy of `content/dev-pack-v2`. They did not edit the source fixture. They used real Playwright browser sessions and the real content service. No mocked API acceptance was used.

## Implemented coverage

`apps/foundry/e2e/m11f-api.spec.ts` covers the real API path for:

- draft creation and published-pack exclusion before Publish;
- exact stable IDs, PackV2 content hash, per-asset SHA-256, asset byte length, and revision asset bytes;
- distinct source PNG bytes for a same-ID replacement;
- content-addressed source originals after replacement, failed publication, and draft removal;
- Validate, Publish, and immutable last-good readback;
- corrupt PNG, invalid PNG, oversized upload, and missing animation-frame rejection;
- stale concurrent draft mutation and dependency-conflict removal;
- reference image round trip outside runtime revision assets;
- unsafe historical and draft asset access rejection;
- Revert to retained source content.

`apps/foundry/e2e/m11f-foundry.spec.ts` covers the real Foundry UI path for:

- library search by stable ID and display name;
- unit/building filtering and disabled-entry enable/disable;
- new unit creation with an original fixture PNG;
- Save draft, Validate, Preview, Publish, reload persistence, and Revert;
- exact UI-created ID, fields, hashes, and source bytes;
- same-ID source replacement with distinct original PNG bytes;
- world scale and idle/move FPS changes;
- undo/redo and dirty-navigation confirmation;
- duplicate, dependency-aware remove, and force-removal warning;
- reference attachment upload, visibility, runtime separation, removal, and retained source bytes;
- sandbox URL origin derived from the configured game origin.

The existing Foundry flows now use explicit draft/save -> validate -> publish semantics. The existing game audit now requires `replayMatched === true`.

## Commands and results

All browser commands used `PLAYWRIGHT_SERVER_MODE=dev`, ports `20373` (game), `20374` (Foundry), and `20387` (content), an installed Chromium at `/usr/bin/chromium`, and the documented `ignoreDefaultArgs=--disable-dev-shm-usage` host adjustment. Each command copied the original fixture pack to a new `mktemp` directory before starting services. Every browser command used one worker because the content pack is mutable during acceptance.

- `npm ci --ignore-scripts` — exit 0. The lockfile remained unchanged.
- `npm run build` — exit 0.
- `npm test` — exit 0. Workspace results: content-schema 8 files / 40 tests; simulation 1 / 19; navigation 3 / 21; content-server 1 / 18; Foundry 4 / 8; game-web 25 / 76. Total: 42 test files and 182 tests passed.
- `npx playwright test apps/foundry/e2e/m11f-api.spec.ts apps/foundry/e2e/m11f-foundry.spec.ts --project=foundry --workers=1` — 2 passed in 30.2 seconds.
- `npx playwright test --project=foundry --workers=1` — 9 passed in 34.9 seconds. This includes the existing Foundry suites, the updated M1.1-C flows, and both M1.1-F suites.
- `npx playwright test apps/game-web/e2e/interaction-lab.spec.ts --project=chromium --workers=1` — 7 passed in 37.7 seconds. This includes the strict deterministic replay assertion and the existing visual test after the approved snapshot update.
- `npx playwright test apps/game-web/e2e/m11f-visual.spec.ts --project=chromium --workers=1` — 1 passed in 17.6 seconds. It captured both target viewports with `page.screenshot`.

## Visual review and snapshot decision

The first run of the existing visual test before rebaseline failed at `visual capture — army rail and lab framing` because the actual original-art render exceeded the existing approximately 2% snapshot threshold. The actual scene used the original PNG fixture art. The old snapshot used the previous art state.

A fresh review compared the old expected `interaction-lab-framing` and `interaction-lab-army-rail` images with the actual composited screenshots. The concrete differences were the unit and building pixels and the transparent HUD background over the changed scene art. The army-rail control positions, text, spacing, selection counter, formation controls, Select control, and Stop control remained aligned. The 1194x834 capture kept the required controls within the viewport. No layout, input, or camera/render geometry regression was visible.

The mismatch was fully explained by the original-art replacement. The two affected snapshots were updated with actual Playwright captures. No threshold changed. No missing-art snapshot was skipped or weakened.

Updated snapshot hashes:

- `apps/game-web/e2e/interaction-lab.spec.ts-snapshots/interaction-lab-army-rail-chromium-linux.png` — `973aca0edff240e5f3a82f64ab909fb305ce5b14f1f29ac1013b420228b7ce25`.
- `apps/game-web/e2e/interaction-lab.spec.ts-snapshots/interaction-lab-framing-chromium-linux.png` — `d9f4e191ec92790d7c805c03f19f9ff87a9a97af08a1ab4eb61bba053becb911`.

Fresh visual evidence from the final original-art run:

- `/tmp/pastel-m11f-final-visual-artifacts.Ul8uP3/chromium-m11f-original-art-original-art-1280x800.png`
- `/tmp/pastel-m11f-final-visual-artifacts.Ul8uP3/chromium-m11f-original-art-original-art-1280x800.json`
- `/tmp/pastel-m11f-final-visual-artifacts.Ul8uP3/m11f-army-rail-1280x800.png`
- `/tmp/pastel-m11f-final-visual-artifacts.Ul8uP3/chromium-m11f-original-art-original-art-1194x834.png`
- `/tmp/pastel-m11f-final-visual-artifacts.Ul8uP3/chromium-m11f-original-art-original-art-1194x834.json`
- `/tmp/pastel-m11f-final-visual-artifacts.Ul8uP3/m11f-army-rail-1194x834.png`

Foundry workflow evidence from the final focused run:

- `/tmp/pastel-m11f-final-artifacts.lf7lTU/foundry-library-1280x800.png`
- `/tmp/pastel-m11f-final-artifacts.lf7lTU/foundry-library-1194x834.png`
- `/tmp/pastel-m11f-final-artifacts.lf7lTU/foundry-unit-before-save-1280x800.png`
- `/tmp/pastel-m11f-final-artifacts.lf7lTU/foundry-unit-draft-saved-1280x800.png`
- `/tmp/pastel-m11f-final-artifacts.lf7lTU/foundry-unit-published-1280x800.png`
- `/tmp/pastel-m11f-final-artifacts.lf7lTU/foundry-reverted-1280x800.png`

## Diagnostics and limitations

The final visual sidecars reported `init error: none`, no page error, and no failed request. They reported Chromium SwiftShader on this Linux host. The 1280x800 capture reported `FPS 15.0`, average `12.4`, and p99 frame time `209.66ms`. The 1194x834 capture reported `FPS 10.0`, average `12.1`, and p99 frame time `173.99ms`.

The browser also reported the existing Chromium warning `GPU stall due to ReadPixels`. The acceptance capture itself used composited `page.screenshot`; it did not use GL `readPixels` for evidence. The SwiftShader result is not an iPad result and does not prove the 60 FPS device gate.

No new A2, D, or M1.1-F product defect was found. The initial visual failure was a resolved art-only snapshot drift. The only host limitation is the unavailable physical-device performance evidence.

## Separate status

Engineering acceptance: PASS for the tested integrated A2/D surface and the existing game interaction audit.

Human design acceptance: NOT ASSESSED. The visual check is engineering visual-regression review only. It is not AoE2:DE quality proof.

Physical-device acceptance: NOT RUN. The Linux SwiftShader metrics do not represent the landscape iPad target.

Runtime E: NOT INTEGRATED and NOT REVISION-ACKNOWLEDGED. The strict replay test passed. That result is audit evidence only. It does not acknowledge an E revision.

No M1.1 or later roadmap status was advanced.
