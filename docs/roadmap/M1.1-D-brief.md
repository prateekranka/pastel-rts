# M1.1-D Foundry designer workflow

Sole repository /home/bobbyranka/Projects/pastel-rts-m11-editor. NOT spacepixelrts. Use absolute paths/explicit workdir. Routing gpt-5.6-luna/openai-codex/max, fast OFF. No delegation/model launching/profile/security/skill/memory/cron changes. Base aab0981. Read numbered contract and M1.1, M1.1-wave2-contract.md, A/B handoffs, M1.1-source-review.md, actual Foundry source and runtime camera. Preserve Git identity; no roadmap advancement.

Implement M1.1 Foundry workflow within allowed scope: apps/foundry/** excluding e2e; apps/game-web/src/camera/previewProjection.ts only. Backend APIs precisely in wave2 contract are being implemented concurrently; do not change backend/schema or invent incompatible endpoints. Do not edit root package/lockfile or runtime files. Tests for this feature are authorized in Foundry, use existing Vitest toolchain and document root coverage need for A2/coordinator.

Deliver search/filter shared library, reference attachments visible but explicitly separate from game art, same-stable-ID replace, duplicate, enable/disable, dependency-aware remove. Dirty-state warning on navigation/close; bounded undo/redo restores current edit fields and pending image state. Preserve source images; replacement uses draft API with explicit expectedDraftRevision. Do not discard local edits on HTTP 409; explain refresh/reconcile. Reference image route and draft asset route in shared contract. Avoid stale async preview updates and dispose object URLs.

Workflow must visibly separate Draft -> Validate -> Preview -> Publish revision -> Revert. Validation calls /v2/validate after explicit draft save, publishing expected current publication cursor; draft save is not live publish. Revert selects retained revision, confirms effect, preserves originals and acknowledges new publication ID. Status strip shows game/Foundry/content links and actual connection state, current/draft revision, rules-change restart requirement and acknowledged runtime/scenario; poll bounded /v2/acknowledgements with cleanup, no fake status.

Test in Sandbox uses configured game URL plus ?mode=interaction-lab&content=studio, selected stable ID, seed/scenario and actual publication revision. Runtime E owns consuming it. Show honest pending state until runtime acknowledges. Non-coder can replace image, edit scale/clip speed, save/validate/preview/publish/revert/reload without lost IDs or sources. Add clear non-coder steps in handoff.

Gameplay preview: draw actual runtime projection at gameplay zoom and 70-percent. Implement helper based on IsometricCamera and shared constants, not arbitrary 96/64 square fitting. Ground anchor, collision/selection footprint, direction overlays and frame grid toggleable. Label proxy or missing frames rather than fabricate animation. Fix unit sheet-control issue identified by QA: explicit sheet dimensions after image inference must be used in manifest; handle input/change event ordering. Use meaningful units. Compact controls retain practical nonoverlapping touch targets at 1194x834 and desktop 1280x800, with normal scrolling allowed for editor.

Validate via existing app typecheck/tests and direct UI against an isolated backend on 17387, Foundry 17374, game 17373 when API checkpoint available. Do not mock success as engineering proof. Early pure editor tests allowed, document pending backend integration truthfully. Add focused handoff docs/roadmap/M1.1-D-handoff.md with commit(s), files, commands/results, screenshots if captured, shortcomings and walkthrough. Commit passing bounded pieces with M1.1-D prefix. No combat/economy/sim/native changes; no full milestone acceptance claim.
