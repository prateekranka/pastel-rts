# M1.1-F independent integrated Foundry and API acceptance

Sole repository /home/bobbyranka/Projects/pastel-rts-m11-acceptance. NOT spacepixelrts. gpt-5.6-luna/openai-codex/max fast OFF, no delegation, no other model launch, no profile/skills/memory/cron/security/config changes. Read numbered contract/milestone, M1.1-wave2-contract, A2/D handoffs, M1.1-integrated-browser-findings and actual changed source. Base 9e282fc. Preserve Git identity.

Role: fresh independent reviewer and verification implementer. Backend A2 and editor D are integrated; runtime E is still independent and not in this base. Do not claim runtime revision acknowledgement yet. Allowed writes: root package.json ONLY for Foundry root test coverage; playwright.config.ts; apps/game-web/e2e/**; apps/foundry/e2e/**; docs/roadmap/M1.1-F-* only. No product source, snapshots unless explicit condition below, schemas, fixtures, lockfile, native or other scripts. Test files for milestone requirements authorized.

Add Foundry test workspace to root npm test so CI executes it. Update old e2e flows to explicit draft/save -> validate -> publish semantics while preserving their observable validation intent. Test newly created drafts are absent from published pack until publish; verify exact stable IDs, hashes and retained source bytes afterwards. Fix isolated game origin test to share actual configured origin. Remove audit's weak typeof replay boolean assertion in favor of true deterministic replay assertion when possible; runtime E may still require pending fix, record failure rather than loosen. Do not hide/skip failures, loosen thresholds, or rebaseline missing-art snapshots just to get green.

Use real browser UI and real backend on 20373/20374/20387, unique temporary copy of original fixture pack. Existing test helper can reroute content but product studio command supports same custom ports. No mocked API acceptance. Ordinary installed Playwright, no added --no-sandbox/--disable-web-security or security-disabling switches; documented ignoreDefaultArgs --disable-dev-shm-usage acceptable. Capture via page.screenshot, not GL readPixels. Target 1280x800 and 1194x834; practical control bounds and visible labels, scrolling allowed for editor.

Exercise library search/filter, same-ID source replacement (actual distinct original PNG frames), scale and clip FPS changes, undo/redo and dirty navigation, Save draft/Validate/Preview/Publish/Revert, reload persistence, duplicate/enable/remove dependencies, reference attachments kept separate. Exercise invalid/corrupt PNG, oversize upload, missing frames, stale concurrent save, failed publication and exact last-good readback. Test source originals persist across unpublished replacements. Do not manufacture direction animation. Export real screenshots and exact failures; new product defects must go to coordinator for Luna fixes, not quietly fix app source.

Fresh visual review: inspect expected old lab screenshot versus actual original-art fixture output; report specific differences and whether art change alone accounts for mismatch. Only if fully explained and no layout/input/render regression, document approval and update exactly the affected screenshot with actual Playwright capture, no threshold change. This is engineering visual regression review, not human design acceptance or AoE quality proof. If uncertainty, leave failed and report.

Run focused Foundry/browser tests and root tests. Keep honest separate engineering/human/device status. Commit M1.1-F bounded passing test/config changes with docs/roadmap/M1.1-F-handoff.md including actual commands/results, screenshot paths, specific defects and reproduction steps. Pending E integration should not prevent available Foundry/API checks. Do not advance M1.1 or later roadmap.
