# M1.1-Q2 affected resource-lifetime soak

Sole worktree /home/bobbyranka/Projects/pastel-rts-m11-soak-final at integrated product6e4d580 (includes S texture handoff and R favicon). gpt-5.6-luna/openai-codex/requested max fast OFF. Explicit workdir on EVERY terminal call. No delegation/other models/profile/security/skill/memory/cron edits. No product/source/test changes. Write only Q2-prefixed evidence/docs. Preserve all old Q/S artifacts and temporary sources.

Coordinator already passed all workspace checks/tests/build/copied-pack validation, full32 developer browser tests with no retries, and separate production-offline test on this integrated code. Do not redo those. Required remaining affected gate: real20minute publication/replacement resource soak after the proven disposal-order fix. Use a fresh copied fixture, fixed seed42, 25 entities, WebGL/DPR1 and1280x800, 12 normal cosmetic PNG replacements, revision/hash/served-PNG/ack checks, before/after composited captures and live geometry/texture/heap/frame metrics. No other local browser load will be scheduled during the soak. Owned33373/33374/33387; don't reuse Q's hardcoded303xx ports. Stop and read back all owned services.

Reuse the existing Q probe through a NEW temporary copy, not a rewrite of old evidence: coordinator docs/roadmap/M1.1-Q-artifacts/probe-source.txt or /tmp/m11-q-replacement-leak-soak.mjs. Preserve the exact new executed source as non-executable Q2 artifact source.txt. Update all worktree/output/port/source-commit metadata to actual6e4d580/current worktree. Keep target1200000ms and12 cycles; no virtual/shortened passing soak, no forced GC, no counter manipulation, no error suppression. Require final and settled per-cycle renderer resources to return to baseline (expected7 textures/17 geometries); record transient samples rather than hiding them. Surface any real growth or runtime errors as blockers. Physical iPad/60fps remains unverified on SwiftShader.

Correct only known probe errors before running: unit art diagnostics are an object with .assets; building diagnostics are an array. Normalize those actual shapes and require nonempty expected fixture assets ready; no vacuous empty-array pass. Old Q summary also incorrectly reports sampled resource min0 when unique values exclude0, and warmup timing sampleCount1231 includes the entire1231-sample run while measured soak has1200. Audit and correct aggregate clock/phase boundaries and finite min calculations; preserve old raw results unchanged. Use one clock basis or explicit sample phase for non-overlapping warmup/soak metrics. Do not invent or manually synthesize output numbers. If a metric is unavailable, say so. No arbitrary Python execute_code or rewrite of a blocked action; use supported file tools and the existing explicit Node probe workflow.

Read relevant existing source/contracts and run the bounded probe. Prioritize the actual run over broad doc rereads. Then concise M1.1-Q2-handoff.md, raw JSON/captures/logs/source, source preservation and cleanup, and own Q2 commit. No PROGRESS edit or overall milestone claim. Only coordinator decides the gate.
