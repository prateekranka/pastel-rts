# M1.1 routing evidence addendum

Latest requested routing: coordinator gpt-6-astra/openai-codex/medium; workers gpt-5.6-luna/openai-codex/max; fast OFF. No profile config edits or inherited delegation used.

The live coordinator process 3774982 is explicitly launched with `--resume 20260905_153912_92169e --model gpt-6-astra --provider openai-codex --reasoning medium`. The session database row still retains the original xhigh model_config. These are different evidence types: the live command proves the current requested effort; the persisted row is not proof of a returned reasoning effort on resumed calls. No claim that the DB row was updated is made.

Luna smoke session 20260905_154737_a9d0e4 completed an actual request. Worker session rows confirm model gpt-5.6-luna and requested max reasoning with billing_provider openai-codex. Source inspection showed max survives generic effort normalization; exact server-side semantics are not inferred from the Max label. Independent raw request/response envelopes were not captured, so returned server-side effort/tier is not claimed.

Wave 2 session identifiers: A2 20260905_164327_ffe67e; D 20260905_164327_860a99; E 20260905_164327_f0a112. D and E were resumed with explicit same pins after stopping their previous process, preserving edits. D continuation forbids security-disabling browser flags; E continuation routes concrete artifact-sanitization findings back to its owner. No concurrent duplicate writer was launched.
