# M1.1-C: baseline UI audit and isolated browser verification

Sole worktree /home/bobbyranka/Projects/pastel-rts-m11-qa. This is pastel-rts, NOT spacepixelrts. Use absolute paths and explicit workdir. Routing gpt-5.6-luna/openai-codex/max, fast OFF. Do not delegate, launch other models, or edit profiles/config/skills/memory/cron/security. Read numbered roadmap contract and M1.1 milestone verbatim. M1.1 only.

Role: independent verification implementer. Base 6be785fe33429cae42f6d4dabd15660a91309827. Allowed changes playwright.config.ts, apps/game-web/e2e/**, apps/foundry/e2e/** and docs/roadmap/M1.1-C-* only. No product implementation, schemas, root package/lockfile, apps source, scripts or fixture mutations. Test-only files explicitly authorized by M1.1 contract. A owns content schema/server; B owns fixture packaging/scripts/root lockfile/Vite config. Use temporary pack directory and ports 14373 game, 14374 Foundry, 14387 content. Port 8787 is occupied by unrelated Hermes Web UI PID 3089063; never kill or reuse it. Root baseline Playwright currently fails on that port collision. Browser verification must use isolated configuration, not weakening assertions. Do not edit server code or shared Vite configuration; use existing CONTENT_PORT if available and independently scoped reverse proxy only if needed. Communicate exact required env/proxy change in handoff if product configuration prevents isolation.

Goal: exercise existing M1 UI before integration: PNG and sprite-sheet import, buildings, Test in Sandbox, unit selection/movement animation, blockers, save/load/replay, initial content and hot reload. Capture real composited screenshots, console/network errors, reproducible defect notes. Attempt root Playwright suites in isolation; report real failures without modifying snapshots or threshold loosening. Add supported configuration overrides for port/test-pack isolation and Chromium path/launch arguments if needed, retaining default behavior. No broad test rewrite. All new helpers must support this milestone's existing requirements.

Inspect installed Chromium first. Known host caveat: /usr/bin/chromium launch with Playwright may require ignoreDefaultArgs ['--disable-dev-shm-usage']; do not disable security controls. Distinguish launch failures from product defects. Capture actual rendered screenshot (not GL readPixels). Performance evidence is CPU/SwiftShader unless renderer proves otherwise, not iPad proof. No physical-device claims.

Current baseline root typecheck, lint, tests and build passed in coordinator. Missing original art fixtures are being addressed by B; document baseline honestly. New worktree may need npm ci. Keep test artifacts under docs/roadmap/M1.1-C-artifacts or ignored test-results; no private source/credentials in captures. Record commands/results, screenshots, exact source bugs and minimal fixes for later Luna tickets, commit any passing isolated test configuration with M1.1-C prefix. End with handoff docs/roadmap/M1.1-C-handoff.md. Do not fix runtime or advance milestone.
