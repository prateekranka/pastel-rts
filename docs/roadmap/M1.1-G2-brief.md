# M1.1-G2 non-empty user-command reproduction gate

Continue in existing /home/bobbyranka/Projects/pastel-rts-m11-bundle from commit130d385, preserving valid work. Same gpt-5.6-luna/openai-codex/max fast OFF. No delegation/profile/security/other-model changes. Coordinator integrated G as4fc3cf7 and independently passed6unit tests and2browser tests. No restart/rewrite.

Coordinator review: browser test currently exports an idle scenario without issuing a user command, and only checks commands truthiness. That does not prove bug reproduction after real interaction. Strengthen same m11g-bundle.spec.ts: select actual unit and send move through UI/pointer (reuse existing interaction-lab test observable helpers), observe accepted command and movement, optionally place blocker if readily stable, then export. Require a non-empty exact ordered command array, accepted command outcome and nontrivial checksum sequence; reproduce in fresh context and compare actual full sequence. Keep refusal preservation case. Do not use injected fake command logs or fabricated checksums. Retain6unit tests and their nonempty command path.

Own same G files; ScenarioController.ts may be changed only for a demonstrated bug this new acceptance exposes, with regression proof and preserving simulation/navigation packages. H still owns PublishedContentClient/GameApp/HUD/runtime config; do not touch them. No other files except docs/roadmap/M1.1-G2-*; no PROGRESS.md/numbered uploads. Use unique M11G_ARTIFACT_DIR and disposable copied fixture/213xx ports, ordinary configured Playwright without unsafe flags. Commit M1.1-G2, write concise handoff, exit. This is a focused coverage/completion correction, not an invitation to expand systems. If the test fails for a real out-of-scope dependency, record exact evidence instead of weakening it.
