# M1.1-D2 projection footprint correction

Sole worktree /home/bobbyranka/Projects/pastel-rts-m11-projection. gpt-5.6-luna/openai-codex/max fast OFF. No delegation/model/profile/security/skill/memory/cron changes. Base current integrated editor. Read numbered M1.1 contract, apps/game-web/src/camera/previewProjection.ts, actual IsometricCamera, UnitRenderSystem and Foundry preview consumer/tests. NOT spacepixelrts.

Coordinator review found projectGroundRadius uses hypot of projected world-X and world-Z edge vectors as screen ellipse radii. At symmetric isometric azimuth both lengths are equal, which yields a circle, not the projected ground circle's horizontal/vertical extents. Goal: correct preview ground collision/selection footprint using actual projection. Allowed files: apps/game-web/src/camera/previewProjection.ts and apps/foundry/src/previewProjection.test.ts ONLY, plus docs/roadmap/M1.1-D2-handoff.md. Do not change camera/input/simulation or other workers' files. Preserve Git identity.

Use exact projected horizontal and vertical radius extrema of the world ground circle (or a sampled/projected polygon only if consumer contract must change, then report rather than widening scope). For orthographic camera, screen X extent is sqrt(dxWorldX^2 + dxWorldZ^2), screen Y extent analogous; derive from actual camera projection vectors, not hardcoded angles. Keep cell/world unit conversion consistent. Test observable footprint projection against sampled ground points at gameplay/default and 70-percent, with different viewport aspect ratios. The test must fail current symmetric-circle implementation. No threshold loosening. Run Foundry tests and app typecheck. Commit M1.1-D2 fix and handoff exact commands/results. Stop after bounded correction; full M1.1 gates remain pending.
