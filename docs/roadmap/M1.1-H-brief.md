# M1.1-H integrated runtime and publication gate

Sole worktree /home/bobbyranka/Projects/pastel-rts-m11-runtime-gate. gpt-5.6-luna/openai-codex/max fast OFF. Explicit workdir/absolute paths. No delegation, other model, profile/skill/memory/cron changes, security disabling flags or tool-denial bypass. Read numbered M1.1 contracts, wave2 contract, E/F handoffs and integrated-browser-findings. Do not edit numbered uploads or PROGRESS.md. Commit own work M1.1-H prefix; no push. Only M1.1.

Actual E code is now integrated with A2 and D. Coordinator root typecheck/lint/tests/build passed. Its integrated browser run at 6208146: 16 passed, 5 failed, 3 did not run. Native pause, blocker sequence and strict replay now passed on real execution (E's handoff was overly cautious). Remaining failures were old art snapshot, old v1 SSE scene-child assumption, stale draft/publish tests and game-origin expectation. F checkpoint is now integrated and fixes Foundry expectations plus art-only snapshots; verify, do not assume all resolved with E.

Own product integration fixes only in PublishedContentClient.ts, GameApp.ts, diagnostics/Hud.ts and runtime/config.ts, plus apps/game-web/e2e/** EXCEPT m11g-bundle.spec.ts (G owns it), necessary existing focused runtime test files or new PublishedContentClient.test.ts, and docs/roadmap/M1.1-H-*. Do NOT edit createInteractionLab.ts or qa/bugBundle.ts (G owns), root package/lock, backend/schema/Foundry, sim/navigation/native/camera. If a real defect needs outside ownership, document exact root cause and stop that subcase rather than make an unauthorized edit; continue independent checks.

Implement real observable tests and fix in-owned-path root causes for initial published studio load, hash/asset identity; cosmetic same-ID replacement while active and repeated replacement/disposal; SSE reconnect with exact newest revision resync; rules change requires explicit restart and actual acknowledgement; pin historical revision; correct scenario and requested spawn ID from Foundry launcher. Test expected draft isolation and failed publication preserve active content. Include revision A/B same seed and commands equality/refusal semantics. Replace obsolete M1.1-C v1 SSE test with explicit published v2 runtime behavior, not child count. No mocks for integration acceptance. Do not loosen visual thresholds: inspect actual screenshots before updating any new E UI-driven drift. Capture 1280x800/1194x834 composited screenshots and real console/request/error/resource/performance diagnostics. Distinguish SwiftShader from physical device.

Run full browser suite on disposable fixture copy and ports 22373/22374/22387. Run production preview with content service stopped and assert no /dev-content or SSE dependency. Ensure test defaults also work in existing CI preview mode, not only dev override. Existing M0/M1 tests must stay meaningful and pass. Do not require unrelated service on 8787 to stop. Ordinary Playwright launch only, no explicit --no-sandbox or unsafe flags. Use configured installed Chromium/ignoreDefaultArgs workaround if necessary.

Hand off code commits, exact commands/results, artifacts and remaining gaps. Do not claim G bundle acceptance (not integrated in this worktree) or whole milestone/human/device pass. Fresh coordinator review will replay final combined build.
