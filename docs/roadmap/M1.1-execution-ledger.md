# M1.1 execution ledger

Scope: M1.1 only. M2–M7 inactive. E1/N1 require approval. Numbered uploads remain unchanged.

## Routing override

Latest user instruction supersedes model names in numbered uploads: coordinator gpt-6-astra/openai-codex, medium; implementation and independent review gpt-5.6-luna/openai-codex, requested max. Fast mode off. No profile changes or inherited delegation.

Coordinator process observed with --model gpt-6-astra --provider openai-codex --reasoning medium. Luna CLI smoke succeeded: session 20260905_154737_a9d0e4, no tools, exit 0. Read-only state.db query returned model gpt-5.6-luna, billing_provider openai-codex, model_config reasoning_config {enabled:true,effort:max}. This is persisted runtime metadata, not model self-identification. Raw provider response metadata has not yet been inspected. Installed agent/reasoning_effort.py explicitly preserves max for gpt-5.6 selectors. Config lookup showed no fallback_model; agent.service_tier printed no value. No fast option was supplied.

## Preserved starting point

Branch docs/roadmap-m1-1 at 6be785fe33429cae42f6d4dabd15660a91309827. git fetch origin succeeded. One worktree; no tmux server and no M1.1 worker CLI observed. docs/roadmap remains untracked user input. No duplicate writers started.

## Existing verification

Earlier execution in this session ran npm run typecheck && npm run lint && npm test && npm run build successfully on the unchanged M1 prerequisite. This is baseline evidence, not M1.1 engineering acceptance. No physical-device result exists.

## Gates

ENGINEERING: pending implementation and integrated verification.
HUMAN DESIGN ACCEPTANCE: pending user review.
PHYSICAL-DEVICE VALIDATION: pending actual iPad and OS identification and release-device test.
