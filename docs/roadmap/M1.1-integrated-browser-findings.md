# M1.1 integrated browser gate, foundation checkpoint

At root through 1313d29, coordinator ran full isolated browser suite, not a worker self-report.
Command: PLAYWRIGHT_GAME_PORT=14373 PLAYWRIGHT_FOUNDRY_PORT=14374 PLAYWRIGHT_CONTENT_PORT=14387 PLAYWRIGHT_CONTENT_PACK_DIR=$(mktemp -d /tmp/pastel-m11-coordinator-e2e.XXXXXX) PLAYWRIGHT_SERVER_MODE=dev PLAYWRIGHT_CHROMIUM_PATH=/usr/bin/chromium PLAYWRIGHT_IGNORE_DEFAULT_ARGS=--disable-dev-shm-usage npm exec playwright test -- --workers=1

Result: 16 passed, 5 failed, 1 skipped (offline-only case), 2 did not run (serial Foundry audit). Engineering gate fails.

Failures to route to Luna:
- Native pause: paused tick delta 5, expected less than 3. Runtime E investigates without loosening threshold.
- Lab visual capture: 26316 pixels changed, ratio 0.03 over 0.02 threshold. New original art deliberately replaces old missing-art proxies. Need fresh Luna visual review of actual/expected screenshots before legitimate snapshot update; do not loosen threshold.
- Foundry v2 API test expects newly saved draft unit/building in published /pack?schema=2. New explicit publish contract requires test to verify not published, call publish with expected revision, then verify published. Preserve validation assertion intent.
- Sandbox origin test expected 5173 but configured actual game is 14373. Test needs to consume same isolated origin contract as config (not hardcoded original port).
- Foundry audit expects saved draft immediately in published pack. Update to actual Draft->Validate->Preview->Publish workflow with UI D when integrated; do not simply remove assertion.

A/B foundation blocker/save/replay audit passed in this run, unlike earlier baseline worker run. This is not proof of replay correctness: audit currently only asserts replayMatched is boolean, which must be strengthened to actual equal checksum behavior for identical inputs after runtime fixes.

Separate production offline run passed (1 test) with PLAYWRIGHT_SKIP_CONTENT_SERVER=1 and preview mode. Composited screenshot: docs/roadmap/M1.1-C-artifacts/chromium-production-offline-bundled-pack-offline.png. Visible fixture sprites replaced generic rectangles. Diagnostics overlap the minimap and are not a clean HUD acceptance capture. Linux timing at cold startup is not physical device performance proof.
