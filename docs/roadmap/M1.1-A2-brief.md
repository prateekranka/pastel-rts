# M1.1-A2 backend integration fixes

Sole repository /home/bobbyranka/Projects/pastel-rts-m11-backend2. NOT spacepixelrts. Explicit terminal workdir and absolute paths. Read docs/roadmap/M1.1-wave2-contract.md and numbered contract/milestone and A/B handoffs. Routing gpt-5.6-luna/openai-codex/max, fast OFF. No delegation/model launch/profile/security changes. Base aab0981. Preserve Git identity. Complete only bounded A2, not whole roadmap.

Implement defined wave2 backend routes exactly. Existing APIs preserved. Ensure GET publication validates migrated current fixture; unknown/missing assets visible errors. Original upload preservation must cover EVERY draft replacement, even multiple unpublished uploads under same ID. Store content-addressed original PNG bytes before mutable draft path writes, retain across failed publish/revert/remove, never place reference images in runtime pack. Add actual tests for two unpublished replacements and failure rollback. Historical routes serve metadata-allowlisted immutable assets only; reference image route serves only registered attachment. Bounded acknowledgements volatile and explicitly not authoritative gameplay state. /v2/validate has no publication side effect. Validate stale draft separately from published cursor.

Review publication transaction failure paths and add injected filesystem failure tests where useful (no weakening guards). No external symlinks/path traversal or secrets. Existing optional preconditions allowed only for compatibility; new UI sends explicit values. Enforce HTTP byte limits while receiving, decoded PNG size limits before allocation; keep originals immutable.

Own root package.json only to add qa:scenario -> tsx scripts/qa-scenario.ts and qa:bundle -> tsx scripts/qa-bundle.ts (E owns their implementation); add Foundry test script coverage if app adds tests using existing vitest tooling. Do not invent stub scripts. Existing studio scripts need correct VITE_GAME_WEB_ORIGIN and VITE content/status URLs when custom ports used; game links must target studio mode. Ensure clean child-group shutdown even if npm parent exits first; cancellation during readiness must settle promptly. Test port collision with unrelated listener preserved, SIGTERM startup and steady-state exit; no leaks. Temporary option must be clearly named/handled without deleting user data. Do not change runtime source, editor source, fixtures, QA test config or native files.

Run focused schema/server tests, root typecheck/lint/tests as applicable; direct HTTP tests isolated pack and ports 18387/18373/18374. Tests authorized for stated milestone requirements. Commit M1.1-A2 checkpoint and docs/roadmap/M1.1-A2-handoff.md with exact commands/results, APIs, commits, remaining defects. Do not claim full milestone pass or physical tests.
