# M1.1-I studio entry and non-coder walkthrough

Sole worktree /home/bobbyranka/Projects/pastel-rts-m11-studio-gate. gpt-5.6-luna/openai-codex/max fast OFF. No delegation, alternative models, profile/skills/memory/cron edits, security disabling or tool-denial bypass. Explicit workdir/absolute paths. Read numbered contracts, wave2, E/F handoffs and scripts/dev-studio.mjs. Do not edit numbered uploads or PROGRESS.md. Commit M1.1-I prefix, no push.

Own only scripts/dev-studio.mjs, scripts/sync-web-build.mjs if an actual packaging defect is found, and docs/roadmap/M1.1-I-* plus docs/roadmap/M1.1-WALKTHROUGH.md. Other workers own runtime, test files and root package. Do not create unrelated tests or edit foreign paths. This is a focused live-startup verification and documentation task, not a rewrite.

Verify root dev:studio with disposable fixture and ports 23373/23374/23387, content health plus game and Foundry pages, meaningful collision/error behavior, interrupt clean shutdown with all owned ports freed and no child leaks. Inspect default printed game URL: runtime now deliberately defaults to bundled content unless ?content=studio. The primary studio URL must select studio published content explicitly, or publishing from Foundry is disconnected. Fix this root cause if present; retain separate clearly labelled bundled/offline preview URL. Preserve normal standalone bundled runtime defaults. Do not stop unrelated service on 8787. No delays instead of health checks; bounded readiness checks are fine.

Write a self-contained non-coder walkthrough using exact actual UI labels/routes from source and running build: start, open Foundry, select stable ID, replace image, change world scale and idle/move FPS, save draft, Validate, Preview, Publish, observe revision/acknowledgement or required restart, Revert, reload. Document source preservation and Draft versus Published. Describe fixture offline launch using production preview and relative iOS bundle. G's browser bug export/import controls are in progress: mark that section awaiting integrated labels rather than inventing them. State exact content:validate / qa:scenario / qa:bundle commands using E CLI input parsing; root aliases are being added by G. Include source fixture scenario/map/command file choices that actually exist, commands requiring no paid services or downloads, known physical-device/human gates and current local SwiftShader limits.

Capture actual launcher stdout, exact URLs, port evidence and commands/results in M1.1-I-handoff.md. Do not claim whole engineering pass or native device run. No final artifact seal yet because G/H code is still being integrated by coordinator. Finish bounded scope, hand off and exit.
