# M1.1 coordinator source-review findings

These are source findings, not substitute UI or device evidence. Base 6be785fe33429cae42f6d4dabd15660a91309827.

1. GameApp.startInteractionLab/loadPackV2 always fetches bundled content/dev-pack-v2/pack.json. refreshLabPack uses the same source on Foundry events. Authoring and actual lab data are disconnected. Runtime lane must load explicit published local content in studio mode, retain bundled offline mode, and expose revision acknowledgement/restart requirement.
2. ContentHotReload only forwards events whose type includes archetype and permanently closes EventSource on error. Runtime lane must support publication events and reconnect/resync without duplicate subscriptions.
3. SpriteAtlasCache.ensureLoaded caches solely by asset path; getForArchetype inserts a placeholder while async loading can still be pending. Failed loads silently create plain rectangles. Runtime lane must generation-guard replacement, avoid stale source reuse, dispose replaced and late-arriving textures, and show visible missing art failure rather than claiming completion.
4. ScenarioController holds an immutable constructor pack. exportSaveDocument lacks map and exact revision identity. importSaveDocument does not check packHash and reuses currentMap. Runtime/QA lane must reproduce exact pinned content/map/commands or fail visibly, not replay against unrelated state.
5. Foundry drawScaledGameplayPreview fits arbitrary bounds to a 96/64 square. This is not the runtime camera projection or 70-percent gameplay scale. Editor lane must use shared runtime camera projection math and truthful physical sprite dimensions, overlays and meaningful scale units.
6. Vite preview middleware serves repository source pack before packaged dist. Packaging lane must remove source fallback from production preview so offline verification proves the artifact.
7. Root Playwright attempt failed before tests: port 8787 belongs to unrelated Hermes Web UI (python PID 3089063). Preserve that service. QA lane owns isolated ports 14373/14374/14387 and isolated temp pack.

PR #3 refreshed through public REST: open, unmerged, head 6be785fe33429cae42f6d4dabd15660a91309827; base main f112a046bb5158bb844121c4b88d9fbf53331bd0. gh auth status currently reports no authenticated hosts. Publication is not claimed.
