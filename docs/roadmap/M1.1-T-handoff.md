# M1.1-T handoff — stable lab typography

Status: IMPLEMENTED AND LOCALLY VERIFIED. Scope remains M1.1 only.

Repository: `/home/bobbyranka/Projects/pastel-rts-m11-typography`
Branch: `m1.1/typography`
Base before T source change: `24924cab7b8bd7cb2ce279de93380e49837aa480`

## Conclusion

The lab layout root cause was the platform-selected `ui-sans-serif,system-ui,sans-serif` stack. Its metrics differed between the CI Linux browser and the approved lab framing. The wider metrics wrapped `Apply seed` and `Place` onto additional lines.

T made the approved product correction in exactly two declarations in `apps/game-web/src/sandbox/createInteractionLab.ts`:

- `.pastel-lab-tools` font stack;
- `.pastel-lab-tools button, .pastel-lab-tools select, .pastel-lab-tools input` font stack.

Both now use `Arial, 'Liberation Sans', sans-serif`. The root remains `13px/1.2`. Control minimum dimensions remain `44px`. No layout rule, control markup, behavior, simulation, snapshot, threshold, test, configuration, or fixture changed.

## Scope and preservation

- Product source diff: one file, two declarations only.
- `src/sim.ts` and other simulation/navigation code: untouched.
- Playwright tests and snapshots: untouched.
- Framing threshold: unchanged at `0.02` in the existing test.
- `PROGRESS.md`: not edited; it remains coordinator-owned.
- No main worktree was written.
- No push was performed.
- No new font was downloaded. No licence or infrastructure change was made.

## Font evidence

Command:

```text
fc-match Arial
fc-match 'Liberation Sans'
fc-match -f '%{family}|%{style}|%{file}\\n' Arial
fc-match -f '%{family}|%{style}|%{file}\\n' 'Liberation Sans'
```

Result on the verification host:

```text
Arial -> Liberation Sans -> /usr/share/fonts/liberation/LiberationSans-Regular.ttf
Liberation Sans -> Liberation Sans -> /usr/share/fonts/liberation/LiberationSans-Regular.ttf
```

The direct browser probe reported `document.fonts.check('13px Arial') === true` and `document.fonts.check('13px "Liberation Sans"') === true` at both target viewports. Computed root style was `Arial, "Liberation Sans", sans-serif`, `13px`, `15.6px`.

## Existing gates already passed

These gates passed earlier in the same T worktree. They were not rerun during the final continuation, as requested.

```text
npm ci
Result: exit 0; 212 packages added; 0 vulnerabilities.

npm run typecheck
Result: exit 0; all six workspace typechecks passed.

npm run lint
Result: exit 0; configured game-web ESLint passed.
```

Focused existing framing command:

```text
env -u PLAYWRIGHT_CONTENT_PACK_DIR -u CONTENT_PACK_DIR -u PLAYWRIGHT_GENERATED_CONTENT_PACK_DIR CI=true PLAYWRIGHT_GAME_PORT=29373 PLAYWRIGHT_FOUNDRY_PORT=29374 PLAYWRIGHT_CONTENT_PORT=29387 PLAYWRIGHT_SERVER_MODE=dev PLAYWRIGHT_CHROMIUM_PATH=/usr/bin/chromium PLAYWRIGHT_IGNORE_DEFAULT_ARGS=--disable-dev-shm-usage npx playwright test apps/game-web/e2e/interaction-lab.spec.ts --project=chromium --grep 'visual capture — army rail and lab framing' --workers=1 --reporter=line
```

Result: `1 passed (15.6s)`. The approved snapshot was preserved. No threshold changed.

Existing M1.1-F unmasked capture command:

```text
env -u PLAYWRIGHT_CONTENT_PACK_DIR -u CONTENT_PACK_DIR -u PLAYWRIGHT_GENERATED_CONTENT_PACK_DIR CI=true M11F_ARTIFACT_DIR="$PWD/docs/roadmap/M1.1-T-artifacts-run1" PLAYWRIGHT_GAME_PORT=29373 PLAYWRIGHT_FOUNDRY_PORT=29374 PLAYWRIGHT_CONTENT_PORT=29387 PLAYWRIGHT_SERVER_MODE=dev PLAYWRIGHT_CHROMIUM_PATH=/usr/bin/chromium PLAYWRIGHT_IGNORE_DEFAULT_ARGS=--disable-dev-shm-usage npx playwright test apps/game-web/e2e/m11f-visual.spec.ts --project=chromium --workers=1 --reporter=line
```

Result: `1 passed (16.4s)`. The test captured unmasked composited page screenshots at `1280x800` and `1194x834`, with all lab controls present and each required HUD region inside its viewport.

## Final direct runtime probe

The corrected temporary verifier was run exactly once after correction:

```text
node /tmp/m11-t-typography-verify.mjs
```

The verifier used the existing worktree, default disposable copy of `content/dev-pack-v2`, isolated game/content ports `29373`/`29387`, `/usr/bin/chromium`, and `ignoreDefaultArgs: ['--disable-dev-shm-usage']`. It started only its owned local services and stopped them in `finally`.

Result: exit `0`. Both viewports passed.

Observed at both `1280x800` and `1194x834`:

- root bounds: `x=12`, `y=12`, `width=520`, inside the viewport;
- five lab rows;
- 19 visible lab controls;
- all visible controls were at least `44px` high and `44px` wide and stayed inside the viewport;
- `Apply seed` and `Place` stayed on the first control line instead of gaining the CI-only extra wrap;
- the existing constrained `Nav debug` placement remained on the second line of its spawn row;
- all controls resolved the declared Arial/fallback family and `13px` size;
- primary canvas dimensions matched the viewport; two canvases were present;
- no page errors, failed requests, or application HTTP errors were reported by the runtime probe.

The first direct capture also recorded the browser default request for the absent `/favicon.ico` as a `404` console notice. This is outside the game runtime and typography path. The second viewport recorded no such notice. The existing M1.1-F sidecars recorded no error diagnostics; they contain only known SwiftShader `ReadPixels` performance warnings.

## Evidence

All T evidence is under:

`docs/roadmap/M1.1-T-artifacts-run1/`

Files include:

- unmasked M1.1-F page captures and sidecars for `1280x800` and `1194x834`;
- army-rail captures for both viewports;
- direct typography verifier captures and metrics for both viewports;
- `typography-verification-summary.json`.

No snapshot or threshold was regenerated.

## Remaining external gate

Remote CI run `33976645789` at `bf3049a` was the pre-T run that failed only the framing comparison. T was not pushed and no remote rerun was claimed. The remote framing gate must execute the committed source change under its existing workflow. Physical iPad validation and human design acceptance remain separate M1.1 gates.

Stop at M1.1.
