# M1.1-L integrated framing baseline reconciliation

Status: reconciled. This is a framing-baseline result only. It is not a whole-milestone acceptance result.

## Scope

- Worktree: `/home/bobbyranka/Projects/pastel-rts-m11-final-snapshot`
- Integrated source reviewed: `d23642d`
- Allowed product artifact changed: `apps/game-web/e2e/interaction-lab.spec.ts-snapshots/interaction-lab-framing-chromium-linux.png`
- Allowed evidence added: `docs/roadmap/M1.1-L-artifacts/**`
- No product source, test source, mask, threshold, configuration, simulation, navigation, or service files were changed.
- No new test file or helper was added.

## Evidence

The historical `test-results/interaction-lab-interactio-d1673-—-army-rail-and-lab-framing-chromium/` directory was not present in the main repository when this work started. I ran the existing test independently in this worktree with the brief's ports, Chromium path, security-preserving browser flags, and automatic disposable fixture.

Before reconciliation, the existing test failed at `apps/game-web/e2e/interaction-lab.spec.ts:278`:

- `31,101` pixels differed.
- Reported ratio: `0.04`.
- Existing per-test tolerance: `maxDiffPixelRatio: 0.02`.

The preserved expected, actual, diff, and Playwright failure context are:

- `docs/roadmap/M1.1-L-artifacts/expected-before.png`
- `docs/roadmap/M1.1-L-artifacts/actual-integrated.png`
- `docs/roadmap/M1.1-L-artifacts/diff-integrated.png`
- `docs/roadmap/M1.1-L-artifacts/failure-context.md`

The non-zero diff bounding box is `520x161+12+279`, or inclusive coordinates `x=12..531`, `y=279..439`. All changed components are in the upper-left lab-tool area:

- The integrated G bug-bundle row replaces the old revision row at approximately `y=279..322`.
- The G help text occupies approximately `x=12..531`, `y=329..389`.
- The existing revision controls move to approximately `y=396..439`.

The diff does not contain a terrain, camera, unit, building, minimap, or gameplay change. The fresh actual and expected images show the same scene composition. The changed controls are intentional source changes from G, retained in the integrated build:

- `apps/game-web/src/sandbox/createInteractionLab.ts:885-889`
- G source commit: `130d385`
- G2 source commit: `bb47c86`
- H integration commit: `d23642d`

The H snapshot was therefore stale for the combined G+H build. Updating it is permitted by the M1.1-L brief.

## Reconciliation

The framing snapshot now matches the fresh integrated actual capture.

- Reconciled snapshot SHA-256: `9f1c0ed8b92666e0da4b781c1a53e0c1f9bf46d357afb1b70f67741c06337d92`
- Historical expected SHA-256: `5f7beeeea6bef960010d9bc4d34b10c41fdd77f26a84fd9611b64c3de2d41159`
- Historical actual SHA-256: `9f1c0ed8b92666e0da4b781c1a53e0c1f9bf46d357afb1b70f67741c06337d92`
- Historical diff SHA-256: `49272a119b399425740da7139cfcd22dd13d1372ad1b4d8c10a545b629a1a393`

## Verification

Command:

`env -u PLAYWRIGHT_CONTENT_PACK_DIR -u CONTENT_PACK_DIR -u PLAYWRIGHT_GENERATED_CONTENT_PACK_DIR PLAYWRIGHT_GAME_PORT=27373 PLAYWRIGHT_FOUNDRY_PORT=27374 PLAYWRIGHT_CONTENT_PORT=27387 PLAYWRIGHT_SERVER_MODE=dev PLAYWRIGHT_CHROMIUM_PATH=/usr/bin/chromium PLAYWRIGHT_IGNORE_DEFAULT_ARGS=--disable-dev-shm-usage npx playwright test apps/game-web/e2e/interaction-lab.spec.ts --project=chromium --grep 'visual capture — army rail and lab framing' --workers=1 --reporter=line`

Result after reconciliation: `1 passed (10.6s)`.

The owned ports `27373`, `27374`, and `27387` were free after the run. Unrelated untracked `docs/roadmap/ACTIVE-MILESTONE.md` and `docs/roadmap/README.md` were left untouched.

No M1.1 engineering, human-design, or physical-device gate is claimed here.
